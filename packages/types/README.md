# @ink-sdk/types

Shared TypeScript types for the Ink SDK workspace.

Includes chain targets, dWallet records, signing payloads, receipts, adapter interfaces, storage interfaces, and v0.2 policy types.
